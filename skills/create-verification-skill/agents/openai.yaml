interface:
  display_name: "Create Verification Skill"
  short_description: "Generate a project-local verification skill for real UI, CLI, or service behavior."
  default_prompt: "Use $create-verification-skill to generate a verification skill for this project."
policy:
  allow_implicit_invocation: false
